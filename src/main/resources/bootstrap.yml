spring:
  application:
    name: api-gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: sell-config-server
      profile: dev

server:
  port: 20000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/




